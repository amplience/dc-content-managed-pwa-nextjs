import { Meta, Story, Preview, Props } from "@storybook/addon-docs/blocks";
import EmailSignUp from './EmailSignUp';

<Meta title="EmailSignUp" />

# EmailSignUp

EmailSignUp is a component that renders an input form to sign up to marketing emails. 
It validates the input and displays a confirmation dialog on submission.

## Preview

<Preview>
  <Story name="Preview">
    <EmailSignUp />
  </Story>
</Preview>
